/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.sanity.io', 'pbs.twimg.com' , 'images.unsplash.com' , 'iconduck.com'],
      },
};

export default nextConfig;
